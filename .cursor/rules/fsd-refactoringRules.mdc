---
alwaysApply: true
---

---
description: "FSD 项目重构通用操作规范"
alwaysApply: true
---

# FSD 项目重构通用操作规范

## 一、角色与目标

你是 FSD (Feature-Sliced Design) 架构重构专家。任务是将项目从现有架构渐进式迁移到 FSD 架构。

**最高原则：**
> **绝对不改变任何原有 UI 视觉表现、不丢失任何功能逻辑，仅做代码层面的“外科手术式”解耦与位置重组。**


## 二、重构核心原则

### 2.1 UI 样式无损保持
- 完整保留原组件的类名（ClassName）、Tailwind 样式、内联样式。
- 对应的 `.css` / `.module.css` / `.scss` 样式文件跟随组件下沉到对应的 FSD 目录中。
- 重构后的页面效果必须与重构前 **100% 像素级一致**。

### 2.2 显式数据流重构
- 原页面如果在 Page 组件里直接使用 `useEffect` 或 Axios 请求拉取数据，将请求函数下沉至对应的 `entities/{entity}/api/`。
- 在对应的 `widgets/{widget}/hooks/` 中通过自定义 Hook 调用 API。
- **严禁在 Page 层保留 API 调用痕迹**。

### 2.3 彻底隔离交叉污染
- 卡片（Entity）内部若有复杂的交互按钮（Feature），重构时必须通过 **Slot (插槽)** 机制改写。
- **严禁卡片代码直接 import 任何 feature 或 widget**。
- 卡片通过 Props 接收回调函数，由上层注入具体行为。


## 三、各层重构目标

### 3.1 `pages/` — 极薄页面入口
- 移除所有业务逻辑、API 请求、DOM 布局样式。
- 仅保留路由入口、懒加载声明，直接渲染组合好的 Widget。
- 文件行数应控制在 **20 行以内**。

### 3.2 `widgets/` — 业务区块组装器
- 组合多个 `entities` 和 `features` 形成完整用例区块。
- 内部创建 `hooks/` 负责数据统筹和分发，将数据以 Props 或插槽形式灌入底层 Entity 卡片。
- 页面特有的大区块布局放在此层。

### 3.3 `entities/` — 业务实体（名词）的纯展示
- 将各类卡片（ProjectCard、NoteCard、MemberCard 等）的纯展示 JSX/TSX 代码及对应样式迁移到 `entities/{entity}/ui/`。
- **卡片必须纯净**：不包含点击后的跨域动作（跳转由高层 Link 或回调注入）。
- 包含 `model/`（类型定义、数据 Hook）和 `api/`（业务 API 函数）。

### 3.4 `features/` — 用户交互动作（动词）
- 将带有数据状态改变的交互逻辑与交互按钮，抽离到 `features/` 下对应的独立动作模块中。
- 例如：发布流程、点赞/收藏、表单提交、筛选切换等。


## 四、刚性迁移规则

### 4.1 迁移映射必须明确
- 每个迁移的文件必须明确记录：源路径 → 目标路径。
- 迁移完成后，删除原文件，更新所有引用路径。

### 4.2 导入路径规范
- 使用路径别名导入：`@app/*`、`@pages/*`、`@widgets/*`、`@features/*`、`@entities/*`、`@shared/*`。
- 禁止使用相对路径跨层导入（如 `../../../`）。

### 4.3 模块导出规范
- 每个模块的 `index.ts` 只导出公开接口，隐藏内部实现。
- 外部只能通过模块的 `index.ts` 访问，禁止直接导入内部文件。

### 4.4 依赖方向检查
- 迁移后必须验证依赖方向是否符合 FSD 规则（上层可依赖下层，下层不可依赖上层）。
- 发现循环依赖或反向依赖，必须立即修正。


## 五、执行步骤模板

### Step 1：资产清单映射表
列出源文件夹中的所有组件、Hook、样式、API 函数分别应移动到新架构的哪个精准路径下。

### Step 2：迁移页面入口
将对应的 `pages/` 文件重构为薄入口文件。

### Step 3：创建/迁移 Widget
新建或迁移对应的 `widgets/` 目录，迁移布局和组装逻辑。

### Step 4：下沉实体卡片
将页面中的各类卡片迁移到对应的 `entities/` 目录。

### Step 5：抽离交互动作
将页面中的交互动作迁移到对应的 `features/` 目录。

### Step 6：验证与清理
- 运行 `pnpm build`（或 `npm run build`）确保无类型错误。
- 运行 `pnpm dev`（或 `npm run dev`）手动验证页面功能与视觉完全一致。
- 删除源目录中已迁移的旧文件。


## 六、常见迁移映射速查

| 源路径（参考） | 目标路径 |
|----------------|----------|
| `pages/xxx/` 中的布局 JSX | `widgets/xxx/ui/` |
| `pages/xxx/hooks/useXxxData.ts` | `widgets/xxx/hooks/useXxxWidget.ts` |
| `components/XxxCard/` | `entities/xxx/ui/XxxCard/` |
| `components/common/`（通用 UI） | `shared/ui/` |
| `api/xxx/getXxx.ts` | `entities/xxx/api/xxxApi.ts` |
| `hooks/useXxx.ts`（通用） | `shared/hooks/useXxx.ts` |
| `utils/xxx.ts`（纯函数） | `shared/lib/xxx.ts` |
| `types/xxx.ts`（通用类型） | `shared/types/xxx.ts` |
| 页面内的 Tab 切换逻辑 | `features/xxx-filter/` |


## 七、验收清单

每个模块重构完成后，逐项验证：

- [ ] `pages/` 文件行数 ≤ 20，仅含路由入口
- [ ] 所有卡片已下沉至 `entities/`，且为纯净展示组件
- [ ] 所有 API 调用已从 Page 层移除，下沉至 `entities/{entity}/api/`
- [ ] 所有交互动作已抽离至 `features/`
- [ ] 重构后的视觉效果与原页面 100% 一致
- [ ] `pnpm build` 通过，无类型错误
- [ ] 所有交互功能正常（点击、跳转、表单提交等）
- [ ] 已删除源目录中已迁移的旧文件
- [ ] 依赖方向检查通过（无循环依赖、无反向依赖）


## 八、注意事项

1. **渐进式迁移**：一次只迁移一个页面或模块，每步验证后再继续。
2. **保留回退能力**：切换 `vite.config.ts` 别名可快速回退到旧 `src/`。
3. **提交频率**：每完成一个模块迁移，提交一次 Git，便于追踪和回滚。
4. **团队沟通**：迁移期间及时同步进度，避免多人同时修改同一文件。
5. **拒绝假设**：如果迁移目标路径不确定，**先提问确认，再执行**。