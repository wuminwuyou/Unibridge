你是一位资深的前端 UI/UX 设计师。请为我的全栈项目“Unibridge（一个连接学生、导师和机构的项目招募平台）”设计一个【即时通讯（IM）功能】的视觉原型草图。

【整体视觉风格】
- 极简、高级的日间模式（Light Mode），背景色使用白色，浅色调为主。
- 现代、高级的暗黑模式（Dark Mode），背景色参考深色调（如 #0d1117 或 #121214），辅以优雅的蓝色或紫色作为微弱的渐变点缀色。
- 界面需要极简、干净，带有精致的毛玻璃效果（Backdrop Filter）和清晰的细边框分割线（Border），整体质感对齐 Discord 或 Linear。

【布局架构：三栏式（Three-Column Layout）】
请使用标准的 Flex 布局，从左到右严格划分为以下三栏：

1. 最左侧栏（宽度固定，约 28px - 40px，一个图片图标的宽度 + 4px边距）- 【项目/招募隔离区】
   - 模仿 Discord 的服务器垂直列表。
   - 存放用户加入的各个“项目/招募卡片组”。每个项目用一个圆角矩形图标表示。
   - 顶部有一个“全部消息”的全局聚合图标，底部有一个“新建对话分组”的加号按钮。
   - 存在四个区域：
     * 个人私聊（Direct Messages），排序在最顶部，且不允许被调整到其他地方
     * 实验室、团队群聊（Group Messages），在个人私聊下面，且不允许被调整到其他地方
     * 基于项目分组（Project Messages），在Group Messages下面，使用浅色横线隔开
     * 临时对话（Temporary Messages），排列在最底部，与基于项目分组使用浅色横线隔开
   - 当激活某个项目图标时，左侧有蓝色（light-blue）的指示条。
   - 支持长按拖动改变列表中的排列顺序（如果web允许的话）

2. 中间栏（宽度固定，约 240px - 280px）- 【对话/频道分组栏】
   - 背景色比左侧稍浅，形成视觉层级。
   - 顶部显示当前选中的“聊天标题”（个人私聊、实验室名称、团队名称等）或者“项目名称”及下拉菜单。
   - 如果是实验室、团队或者项目合作的对话，那么初始存在一个固定的群聊，加入到实验室、团队或者项目的用户都会自动加入该群聊（大群）
   - 支持通过大群自由拉取成员：
     * 创建私聊（Direct Messages）：通过点击成员头像开启私聊。
     * 合作小组（Text Channels）：例如 #全体群聊、#前端开发组、#技术方案讨论。
   - 支持未读消息的小红点提示（Badge）。

3. 最右侧栏（自适应剩余宽度）- 【主对话与聊天交互窗】
   - 顶部状态栏：显示当前正在聊天的频道名（如 #前端开发组）或私聊好友名字、在线状态。
   - 中间消息流主体：
     * 区分“左侧接收的消息（白/浅灰文字，深色气泡）”和“右侧发送的消息（白字，蓝色气泡）”。
     * 消息包含用户头像、昵称、角色标签（如：导师/组长/学生）和发送时间。
     * 支持文本、系统卡片通知（如“某某已通过审核加入项目”）。
   - 底部输入框：悬浮于聊天流上方，圆角设计，左侧有加号（发送图片/文件），右侧有发送按钮和表情图标。

【交付要求】
1. 请先简要说明你在交互和视觉层级上（层叠关系、间距、字体大小）的设计思考。
2. 给出一段完整的、开箱即用的前端 HTML+Tailwind CSS 代码原型（如果可以，请使用 React 组件形式表达，代码中用基础的 SVG 图标代替图片），以便我能直接复制在浏览器中预览这个三栏式 IM 布局。