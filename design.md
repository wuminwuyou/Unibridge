## 🎨 Unibridge 主界面（项目与笔记双栏区域）前端设计规范说明书

### 一、 设计主题与色彩管理 (Theme & Color Tokens)

为了贯彻平台“年轻态、高含金量、去噪点”的科技实验室质感，双栏区域的色彩规范如下：

| 视觉元素 | 白天模式 (Light Mode) | 暗黑模式 (Dark Mode) | 设计意图 |
| --- | --- | --- | --- |
| **主画布背景** | `🎨 #F8FAFC` (Slate 50) | `🎨 #0F172A` (Slate 900) | 全站大面积清脆、空灵的留白底色 |
| **卡片面板主体** | `🎨 #FFFFFF` (纯白) | `🎨 #1E293B` (Slate 800) | 微凸、无生硬边框的漂浮式轻量面板 |
| **卡片轻微阴影** | `rgba(0,0,0,0.02)` 极柔和 | `rgba(0,0,0,0.15)` 沉降 | 依靠现代空气感微弱阴影（`shadow-sm`）划分空间 |
| **主标题/核心文字** | `🎨 #1E293B` (Slate 800) | `🎨 #F1F5F9` (Slate 100) | 现代轻量无衬线体（sans-serif），字形极其干练 |
| **正文简介/描述** | `🎨 #475569` (Slate 600) | `🎨 #CBD5E1` (Slate 300) | 保持知性克制的灰度，兼顾长文本阅读行高 |
| **弱元数据/小图标** | `🎨 #94A3B8` (Slate 400) | `🎨 #64748B` (Slate 500) | 卡片底部极淡的社交或属性属性标签字体 |

---

### 二、 设计语言与全局交互 (Design Language & Interactivity)

1. **圆角控制 (Radius)**：大卡片容器（项目、笔记）统一为 `border-radius: 16px (rounded-2xl)`；内部元素使用已经完成了的项目卡片和网格笔记卡片

---

### 三、 区域级界面布局与微观规格

页面主体限制在 `max-w-7xl` 或 `2xl` 视口，内部采用 **`flex` 或 `grid` 进行非对称异步双栏排版（左侧 75% 占位，右侧 25% 占位）**，栏间距设为 `gap-6 (24px)`。

#### 📊 1. 左侧 75% 区域：项目实验室大厅 (Project Laboratory Hub)

由纵向网格（`flex flex-col gap-5`）排列的纯白漂浮项目卡片流组成。卡片使用src\components\ProjectCard


* **`[右: 强动作干预按钮区]`**：
* 水平垂直完美居中一个无缝填充的天蓝色按钮，采用干净无杂乱纹理的 `bg-sky-500` 填充（暗黑模式对应 `dark:bg-sky-600`），上面带高可读性现代简体中文：“看看细节” 或 “申请加入”。



---

#### 📝 2. 右侧 25% 区域：学生项目复盘与经验 (Student Insights Sidebar)

作为高精度的信息聚合侧边栏。顶部带有一行精致的标题与右侧的流线型文字链接 `查看全部 >`。下方垂直堆叠两种形态的纯白、无生硬边框笔记卡片，使用src\components\NoteCard\GridNoteCard

---

### 四、 底部全局交互组件

* 在右侧侧边栏的最底部，固定悬浮或常驻一个全宽、极简线条风格的输入交互引导组件（Action Trigger）：`[ 📝 写下你的项目经验 ]`。
* 整体采用淡蓝色边框或极为低调的 Slate 灰色文字提示，用户点击即可平滑弹窗或跳转至富文本编辑器，极大地赋予了整个双栏布局年轻、互动、双向输出的学术工作坊氛围。