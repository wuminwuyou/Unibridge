你现在是一位精通现代全栈前端开发的资深 UI/UX 架构师。请为一款专为学生项目众包平台定制的、具备“多域低频、司法级留档、动态生命周期控制”特征的工业级即时通讯（IM）组件，生成高保真的前端界面设计指南与布局方案。

本设计必须全面适配 Tailwind CSS 的样式体系，能同时满足 AI 视觉渲染（如 ChatGPT 预览图）与 IDE（如 Cursor）的代码直接生成，且不得改变任何既定语义与设计细节。

【核心设计视觉风格（借鉴 Discord 极客工程感）】
- 整体风格：高密度、强组织感的极简极客风格。
- 顶部导航栏：完全隐藏（Hidden Top Navbar），最大化垂直工作空间。
- 双模式色彩规范 (Tailwind Classes)：
  * Dark 模式（主视觉）：背景采用极暗灰黑调（bg-zinc-950 / bg-neutral-900），文字采用钛白与冷灰（text-zinc-100 / text-zinc-400），激活态使用高饱和度极客蓝/绿色（text-emerald-400 或 text-sky-400）进行科技感点缀。
  * Light 模式：背景采用高级明亮灰白（bg-slate-50 / bg-gray-100），文字采用深邃炭黑（text-slate-800），保持清晰的工程骨架线条（border-slate-200）。
- 材质感：大量使用毛玻璃微透效果（backdrop-blur-md）、细腻的半透明边框（border-zinc-800/50）与紧凑的间距控噪（p-2, space-y-1），呈现纯正的工业软件质感。

【全局三栏式（Three-Column Layout）网格物理架构】
界面整体使用 `flex h-screen w-full overflow-hidden` 容器包裹，自左向右严格分为三栏：

---

【第一栏：最左侧·域（Domain）纵向导航条】
- 尺寸与基调：宽度固定（w-20），高密度垂直排列，Dark 模式下背景色最深（bg-zinc-950）。
- 域图标核心规范：
  * 所有域图标默认均为圆角正方形。
  * [团队域] 使用对应的实验室或学生团队真实头像。
  * [项目域] 图标采用浅蓝色背景、毛玻璃材质、内部为项目名称首字（如项目“企业级大模型部署”图标内文字取“企”字，后端会自动生成图片并在开启项目域初始化时写入到数据库中）。
  * [交互特效] 选中的域左侧边缘出现蓝色竖线（类似 Discord 激活态）；鼠标悬浮在任一域图标上时，右侧会弹出浮动气泡显示该域的全称。
- 自上而下纵向布局结构：
  1. 【顶部·核心域】：
     - [熟人私聊/创建群聊域]：特殊圆形高亮图标按钮（`rounded-full hover:rounded-xl transition-all`），带有未读提示红点（`bg-sky-500 animate-pulse`）。
  2. 【中部·业务域动态滚动区】（`flex-1 overflow-y-auto no-scrollbar`）：
     - [团队域（实验室与学生自建团队）]：方形圆角图标组，带有细微的层级分界线。
     - [项目域（处于活跃生命周期的众包项目）]：采用高对比度带编号或徽章的图标。
  3. 【最底部·非业务域与全局控制区】：
     - [临时对话域（项目探索、实验室咨询私聊）]：采用半透明虚线边框图标（border-dashed），传达“临时性/轻量化”语义。
     - [通知域（系统级各类合规与认证通知）]：固定铃铛矢量图标，带有高亮红点计数器（`bg-rose-500`）。
     - [设置按钮]：固定的齿轮矢量图标（`hover:rotate-45 transition-transform`），紧贴最左侧栏的最底部。

---

【第二栏：中间栏·频道分组与管理工具集成面板】
- 尺寸与基调：宽度动态（通过 Tailwind 的 `w-[clamp(240px,18vw,360px)]` 类似公式计算得出适合的值），借鉴“飞书（Feishu）”的紧凑型分层工作台架构，带有清晰的纵向边框线。
- 从上到下内容排布：
  1. 【顶部·域信息与搜索】：顶部展示当前所在“域”的名称，右侧紧跟一个圆形虚线描边的“+”轻量矢量图标（表示新建对话）。名称与“+”图标下方配置一个紧凑的搜索栏，用于当前域内的关键词全局搜索。
  2. 【中部·通讯组件】：包含当前域下的所有“群组”和“私讯”对话列表。
  3. 【底部·工具组件】：放置所有的项目与实验室管理工具链，支持向底部折叠隐藏。
- 内部纵向布局结构与交互规范：
  1. 【上半部分：群组与私讯对话】：
     - 采用圆角卡片设计（`rounded-lg`），背景默认透明无色，卡片之间上下保持 1px 左右间距，左右保持 2px 左右间距。
     - 鼠标悬停卡片时加上深色阴影效果，选中的对话卡片呈现明显的边框描边激活效果。
     - 布局右侧必须绝对显示最新消息日期（规则：当天显示如“昨天 11:18”，同年显示如“6月11日”，非同年显示如“2025年1月2日”）。标题下方展示单行文本信息预览（如“张三：任务已完成”）。
     - [防挤压边界设计]：如果对话标题 + 日期的总宽度超过卡片容器宽度，则必须自动省略截断标题，确保右侧日期完全完整显示。
     - [群组/私讯视觉区分]：
       * 系统默认大群“项目合作组”（加入该域的成员默认进入）使用默认的圆角正方形树形组织结构图标（后端创建时填充 png）；其他自定义群组图标在创建时由用户自定义，均为圆角正方形。
       * “私讯”（个人一对一通讯）的图标则强制使用通讯对象的真实头像，且形状必须为【圆形】，以此在视觉上与方形群组彻底区分。
  2. 【下半部分：项目与实验室管理工具】：
     - 紧跟在频道对话下方，通过一条精致的暗色灰线（border-t）分隔，包含以下高密度生产力卡片组件：
     - [语言会议室(Meeting)]：会议室（带录制状态）矢量图标。
     - [文档仓库 (Docs)]：文件夹矢量图标，直连项目核心 Wiki。
     - [多维表格 (Base)]：小表格组件图标，字样紧凑，提示跨表数据视图。
     - [里程碑与任务卡片 (Milestones)]：里程碑展示分阶段验收的核心节点与截止时间，下方带缩进展示依附的任务卡片、执行人及状态。
     - [代码仓库 (Repo)]：分支/Git 矢量图标，直连实验室源码库。

---

【第三栏：右侧栏·聊天主看板与动态抽屉控制中心】
- 尺寸与基调：主体占据右侧剩余全部空间（`flex-1 bg-zinc-900`），作为即时通讯与内容交互的绝对主体。
- 内部布局：
  - 【上部】当前激活会话的标题栏、成员人数与在线状态栏。
  - 【中部】高密度的聊天消息时间流（支持标准 Markdown 语法渲染、多语言代码块高亮展示、文件传输流水的司法级 SHA-256 哈希指纹微型浮字防篡改提示）。
  - 【底部】多功能富文本输入区域，右侧集成“发送”按钮，并为具备特权身份的管理人员提供醒目的“一键归档/申请司法结项”特权控制按钮。

请根据以上详尽的 Tailwind 布局语义、极客双模式色彩方案、三栏组件嵌套关系以及【私讯圆形/群组方形】等特定视觉规范，渲染出完美的视觉图预览（若用于 ChatGPT），或直接使用标准合规的 React TypeScript + Tailwind 语法输出整套完备的布局结构与组件代码（若用于 Cursor）。